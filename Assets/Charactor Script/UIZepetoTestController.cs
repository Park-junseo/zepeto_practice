using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIZepetoTestController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void GetUIFloat(Vector2 value)
    {
        Debug.Log("[getUIFloat]"+ value.ToString());
    }
}
